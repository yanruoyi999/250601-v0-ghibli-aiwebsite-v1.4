name: Wealth Video Full 142 Images (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy long-prompt renderer disabled; use v2 short-prompt workflow."
